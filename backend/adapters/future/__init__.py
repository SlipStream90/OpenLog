"""Reserved for future agent adapters (Cursor, Gemini CLI, Cline, ...).

Deliberately empty -- see PRD section 27. Adding an adapter here requires only
implementing the Adapter protocol in `backend/shared/adapter.py`; no change to
the telemetry, database or API layers should ever be needed.
"""
